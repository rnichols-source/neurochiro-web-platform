export const metadata = {
  title: "Seminars | NeuroChiro",
  description: "Find chiropractic seminars and continuing education events.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
