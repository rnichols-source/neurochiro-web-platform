import { Doctor } from "@/types/directory";

// Mock data has been removed in favor of live Supabase data.
// See src/app/(public)/directory/actions.ts for live fetching logic.

export const MOCK_JOBS: any[] = [];
export const MOCK_VENDORS: any[] = [];
export const MOCK_SEMINARS: any[] = [];
export const MOCK_ROI_DATA = {
  views: 0,
  leads: 0,
  conversions: 0
};
